# adrianciencia.github.io

Minimal Astro foundation for the personal website rebuild.

## Commands

- `npm run dev` — start the local development server
- `npm run build` — create the static production output in `dist/`
- `npm run preview` — preview the production build locally
